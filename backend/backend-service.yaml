apiVersion: v1
kind: Service
metadata:
  name: backend-service
spec:
  type: NodePort   # 🔥 CHANGE THIS
  selector:
    app: backend
  ports:
    - port: 5000
      targetPort: 5000
      nodePort: 30008   # any free port